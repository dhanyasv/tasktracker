const Button = ({onClick}) => {
    return (
        <>
        <button type="button" value="DHANYA" onClick={onClick} className="btn btn-primary">Add</button>
        </>
    )
}

export default Button
